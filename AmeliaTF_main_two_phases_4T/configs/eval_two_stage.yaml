type: Single-Airport
ckpt: kbos2

defaults:
  - _self_
  - data: kbos.yaml
  - model: combined_traj_pred.yaml
  - callbacks: default.yaml
  - logger: wandb.yaml # set logger here or use command line (e.g. `python train.py logger=tensorboard`)
  - trainer: default.yaml
  - paths: default.yaml
  - extras: default.yaml
  - hydra: default.yaml

task_name: "eval"

tags: ["two_phases"]

# passing checkpoint path is necessary for evaluation

ckpt_dir: ${paths.base_dir}/checkpoints
mode_ckpt_path: ${ckpt_dir}/${type}/${ckpt}/mode_model/${ckpt}_twophases_4_50.ckpt
traj_ckpt_path: ${ckpt_dir}/${type}/${ckpt}/traj_model/${ckpt}_twophases_4_50.ckpt
# seed for random number generators in pytorch, numpy and python.random
seed: 42

scorer:
  stage: score          # eval | score
  lr: 1.0e-3
  weight_decay: 1.0e-4
  score_head_save: /gpfs/scratch/exy064/ljx/Risk-Assessment/AmeliaTF_main_two_phases_4T/out/${ckpt}/per_mode_scorer_soft.pt
